# App identity
HERO_NAME=x

# Advisor behavior
ADVISOR_OBSERVE=1

# Optional: connect to an existing Chrome instead of launching a new one
# Find this at http://127.0.0.1:9222/json/version (webSocketDebuggerUrl)
BROWSER_WS_ENDPOINT=ws://127.0.0.1:9222/devtools/browser/REPLACE_ME

# Optional: default table to open if your launcher uses it
GAME_ID=REPLACE_ME
